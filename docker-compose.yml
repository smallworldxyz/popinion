# The graph store is now embedded (SQLite, in ./uploads/graph.db) — no Neo4j.
# These headless-browser services are OPTIONAL, only for future X/Facebook
# crawling; Telegram crawling and everything else runs with `cargo run` alone.
services:
  # Primary: LightPanda - 10x faster, 10x less RAM
  # https://lightpanda.io - AI-native headless browser
  lightpanda:
    image: lightpanda/browser:nightly
    container_name: popinion-lightpanda
    ports:
      - "9222:9222" # Primary CDP endpoint
    environment:
      - LIGHTPANDA_DISABLE_TELEMETRY=true
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:9222/json/version || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # Fallback: Browserless Chrome - full compatibility for complex JS
  # Use when LightPanda fails on JS-heavy sites (Twitter, Facebook)
  chrome:
    image: browserless/chrome:latest
    container_name: popinion-chrome
    ports:
      - "9223:3000" # Fallback CDP endpoint
    environment:
      - CONNECTION_TIMEOUT=300000
      - MAX_CONCURRENT_SESSIONS=5
      - ENABLE_DEBUGGER=true
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:3000/json/version || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5


